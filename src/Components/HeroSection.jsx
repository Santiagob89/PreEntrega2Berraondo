import React from 'react';
import ItemListContainer from './itemListContainer';

const HeroSection = () => {
  return (
    <section className= 'hero-section bg-red-700'>
        <ItemListContainer />
    </section>
  )
}

export default HeroSection